<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>High Scores</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            padding: 20px;
        }

        h1 {
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

        table,
        th,
        td {
            border: 1px solid black;
        }

        th,
        td {
            padding: 8px 12px;
            text-align: center;
        }

        #home-btn {
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        #home-btn:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>
    <h1>Top 10 High Scores</h1>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Score</th>
            </tr>
        </thead>
        <tbody>
            <% scores.forEach(function(score) { %>
                <tr>
                    <td>
                        <%= score.name %>
                    </td>
                    <td>
                        <%= score.score %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>

    <!-- Home Button -->
    <button id="home-btn" onclick="window.location.href='/'">Go to Home</button>
</body>

</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forumbar - <%- pageTitle %></title>
    <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
    <div class="header">
        <h1 id="logo" onclick="window.location.href = '/'">Forumbar</h1>
        <% if(locals.loggedIn) { %>
            <a id="profile-button" href="/profile/<%= user.uid %>"><button>Profile</button></a>
            <a id="login-button" href="/logout"><button>Logout</button></a>
        <% } else {%>
            <a id='login-button' href='/login'><button>Login</button></a>
        <% } %>
    </div>
    <div class="container">
        <div class="content">
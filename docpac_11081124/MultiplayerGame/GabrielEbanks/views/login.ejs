<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script>
        function newDoc() {
            let textBoxContent = document.getElementById('nameInput');
            if (textBoxContent.value) {
                window.location.assign("/game?username=" + textBoxContent.value);
            } else {
                alert('Type a Username');
            }
        }
    </script>
</head>

<body>
    <label for="username"></label>
    <input name="username" type="text" id="nameInput" class="required">
    <!-- text-box for username that only allows submissions if text is entered -->
    <button onclick="newDoc()">Login</button>
    <!-- submit button for redirecting the user to /chat if enable submit function has -->
</body>

</html>
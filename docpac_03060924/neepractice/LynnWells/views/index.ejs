<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sussy EJS</title>
    <style>
        .p {
            font-weight: bolder;
            font-family: monospace;
                }
    </style>
</head>

<body>
    <h1 style="font-size: xx-large;">STEVEN!!!</h1>
    <button id="shutdownBtn">KILL</button>

    <script>
        document.getElementById('shutdownBtn').addEventListener('click', () => {
            fetch('/shutdown')
                .then(response => response.text())
                .then(text => {
                    console.log(text); // Log response from server
                })
                .catch(error => {
                    console.error('Error:', error);
                });
        });
    </script>
</body>

</html>
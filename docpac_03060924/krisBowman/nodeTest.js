const express=require("express"); // add express module
const {send} = require("process");

const app=express(); //transfer express into object for furture usage
const PORT = 3000;

app.listen(PORT, (req, res) => {
    console.log("app started");

app.get("/", (req, res,)) => {

});   send 

});
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders</title>
</head>
<body>
    <% if(orders) { %>
        <ol>
            <% for (order of orders) { %>
                <li> <%= order.brand %> - <%= order.quantity %> </li>
            <% } %>
        </ol>
    <% } %>
</body>
</html>
